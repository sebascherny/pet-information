# Database (used by docker-compose)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/pet_information

# JWT secret for session tokens
JWT_SECRET=ygbku1jg3v4kg1hj2vaohlsdgba

# SMTP for appointment reminder emails (optional in dev)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-password
SMTP_FROM=noreply@petinfo.local
